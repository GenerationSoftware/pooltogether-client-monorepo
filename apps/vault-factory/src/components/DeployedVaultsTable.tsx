import classNames from 'classnames'

interface DeployedVaultsTableProps {
  className?: string
}

export const DeployedVaultsTable = (props: DeployedVaultsTableProps) => {
  const { className } = props

  // TODO: implement table here :)
  return <div className={classNames('text-pt-purple-300', className)}>... IN CONSTRUCTION ...</div>
}
